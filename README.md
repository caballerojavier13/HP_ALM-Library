# HP ALM - Java client

## Setup

1. Create a file `alm.properties` in the root folder
2. Fill the file with se following data:

   - port
   - password
   - host
   - domain
   - project
   - username